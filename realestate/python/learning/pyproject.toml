[project]
name = "realfield"
version = "0.1.0"
description = "부동산 실거래가 데모 (자바 Spring Cloud MSA의 파이썬 개념 보존 축소판)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
# 테스트가 realfield 패키지를 import 할 수 있도록 프로젝트 루트를 sys.path 에 추가합니다.
pythonpath = ["."]
testpaths = ["tests"]
